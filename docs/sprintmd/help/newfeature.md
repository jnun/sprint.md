Create a new feature document in docs/features/.

Usage:
  ./sprint.sh newfeature                       # AI-guided Q&A
  ./sprint.sh newfeature "User authentication" # quick template

Without a name, starts an interactive session that asks about users,
requirements, and success criteria, then writes a complete spec.
With a name, creates a blank template you fill in yourself.
