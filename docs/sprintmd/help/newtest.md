Create a test loop in docs/tests/.

A test captures how you validate something you deployed and routes what you
learn back into new work. Define the claim, run your own test, record the
result, then file follow-ups with newfeature or newtask.

Usage:
  ./sprint.sh newtest "Signup converts cold visitors"
