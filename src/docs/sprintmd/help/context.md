Generate an AI context summary for the project (look family — read-only).

Usage:
  ./sprint.sh context

Prints a concise snapshot of ideas, features, plans, tasks, and bugs so an
agent (or human) can load project state without walking every folder.
