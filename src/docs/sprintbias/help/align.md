Analyze alignment between features and their tasks (look family — read-only).

Usage:
  ./sprint.sh align

Compares feature docs under `docs/features/` with tasks under `docs/tasks/`
and reports gaps, orphans, and coverage. Does not edit files.
